# -*- mode: snippet -*-
# name: redux-reducer
# key: redred
# --
const $1 = (state = ${2:initialState}, action = {}) => {
  switch (action.type) {
    case '$3':
      return $4;
    default:
      return state;
  }
}

export default $1;